<Window x:Class="Substitutie_monoalfabetica.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Substitutie_monoalfabetica"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Substituție Monoalfabetică" 
                   FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>

            <TextBlock Grid.Row="1" Text="Cheie: m i p o d s q r a b c f g h j k l n t u v w x y z" 
                   Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,15" FontStyle="Italic"/>

            <TextBox x:Name="TxtInput" Grid.Row="2" TextWrapping="Wrap" 
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto" 
                 Padding="10" BorderBrush="#D5DBDB" Margin="0,0,0,10"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
                <Button Content="CRIPTEAZĂ" Click="BtnCriptare_Click" Width="120" Height="35" Margin="5" Background="#5DADE2" Foreground="White"/>
                <Button Content="DECRIPTEAZĂ" Click="BtnDecriptare_Click" Width="120" Height="35" Margin="5" Background="#58D68D" Foreground="White"/>
                <Button Content="ANALIZĂ FRECV." Click="BtnAnaliza_Click" Width="120" Height="35" Margin="5" Background="#AF7AC5" Foreground="White"/>
            </StackPanel>

            <TextBox x:Name="TxtResult" Grid.Row="4" IsReadOnly="True" 
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" 
                 Padding="10" Background="#F4F6F7" BorderBrush="#D5DBDB" Margin="0,10,0,0"/>
        </Grid>
</Window>
